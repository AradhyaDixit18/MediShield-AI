# Root environment example.
# Frontend build-time variable (used by Vite). For local dev the frontend
# defaults to http://127.0.0.1:8010, so you usually do not need to set this.
VITE_API_URL=

# Backend
CORS_ORIGINS=*
LLM_API_KEY=
