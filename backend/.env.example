# Comma-separated allowed origins for CORS. Use "*" for an open public demo,
# or set to your Netlify URL for production, e.g.:
# CORS_ORIGINS=https://medishield-ai.netlify.app
CORS_ORIGINS=*

# Optional: enables the (future) LLM copilot / RAG layer. Leave blank to disable.
LLM_API_KEY=
